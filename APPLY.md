# Applying checkpoint 95–98

This directory contains only files changed or added by checkpoint 95–98.

Preferred Git-native update:

```bash
git am Navier-Stokes-Cornering-Conjecture-update-95-98.patch
git push
```

The patch was generated from commit `864b8fd` against a local parent tree synchronized to the current public GitHub checkpoint-94 tree. The parent commit SHA itself differs from GitHub because the earlier deltas were committed there independently; the file tree used as the patch base matches the published state.

Alternatively, copy this overlay onto the repository root, inspect `git diff`, then commit normally.
